[project]
name = "job"
version = "0.1.0"
description = "PRACTICE!"
authors = [{ name = "Seonaid", email = "seonaidmcnabb@gmail.com" }]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.6",
    "pytest>=9.1.1",
]
